# 独角数卡西瓜版 v3.0.0-rc.1 候选清单

发布日期：待定

版本定位：

- 正式版前的第一轮候选冻结。
- 不新增功能，只接受回归、安全、测试和文档修正。
- 不直接升级 Laravel / PHP 大版本。

## 必须满足的状态

- 后台壳主承载冻结，不再新增后台批量动作。
- Dcat 只作为登录、认证、中间件、权限白名单和旧入口兼容层存在。
- 支付范围固定为官方支付宝、官方微信、易支付和 Epusdt。
- PayPal、Stripe、Coinbase、Mapay、TokenPay、PayJS、Vpay、Paysapi 不恢复。
- 安全专项清单无未解释高风险项。
- 升级实验状态明确，不把实验分支内容混入 RC。

## 发布前验证

```bash
git diff --check
./scripts/composer74 install --no-interaction --no-progress
./scripts/php74 vendor/bin/phpunit
ADMIN_USERNAME=admin-shell-tester ADMIN_PASSWORD=secret123 ./scripts/smoke-admin-shell
```

## 搜索复核

```bash
rg -n "app/Admin|install.sql|admin/admin|base64:|sk_live_|rk_live_|whsec_|paypal/rest-api-sdk-php|stripe/stripe-php|PayPal|Stripe|Coinbase|Mapay|TokenPay|PayJS|Vpay|Paysapi" README.md docs .env.example .env.local.example tests app routes config database composer.json
```

命中项只允许出现在历史日志、退役清单、release 冻结说明或明确的防回流测试中。

2026-05-31 复核结论：

- 退役支付通道未回到 `routes/common/pay.php`、`composer.json`、样例种子或当前支付实现文件。
- `app/Admin` 与 `install.sql` 命中均属于历史文档、兼容层审计或防回流测试语境。
- `base64:` 命中仅存在于动态生成逻辑、测试断言或安全审计说明中，未作为发布示例密钥出现。
- 后台支付新增/编辑入口已限制为 `/pay/alipay`、`/pay/wepay`、`/pay/yipay`、`/pay/epusdt`，并拒绝退役支付标识。
