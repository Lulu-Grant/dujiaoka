# Dujiaoka 大整改执行方案

## 目标

本方案用于把现代化改造固定成“长周期、低打扰、按阶段持续推进”的执行模式。

最终目标不是只把旧项目修到能跑，而是把它推进成一套：

- 可持续维护
- 不依赖守护进程
- 有核心业务测试护栏
- 支付层可替换
- 后台可逐步脱离 Dcat
- 为 PHP / Laravel 升级做好准备

## 默认执行规则

- 默认由 Codex 连续推进，不在每个小节点停下来征求确认。
- 只有在出现高风险分叉、不可逆操作、外部真实信息缺失时才暂停。
- 每个稳定节点必须：
  - 跑当前回归测试
  - 更新 [refactor-upgrade-log.md](/Users/apple/Documents/dujiaoshuka/docs/refactor-upgrade-log.md)
  - 尽量同步 GitHub `master`
- 每个阶段优先处理“能降低后续返工成本”的任务，而不是只追求可见改动数量。

---

## 当前基线

截至 2026-04-13，已形成的基础能力包括：

- 恢复 PHP 7.4 遗留运行时基线
- 建立核心订单主链测试护栏
- 完成去守护进程第一阶段改造
- 将订单创建、支付完成、履约、通知等服务拆分成独立边界
- 将支付入口、支付回调、第三方 SDK 访问压缩到更清晰的服务层
- 安装现代化已完成收口，`install.sql` 已退出安装主路径、退出 CI，并已从仓库移除
- 建立新的默认主题 `avatar`
- 搭起新后台壳底座，并落地多张真实后台壳资源页与动作页
- 建立 GitHub Actions CI 基线与本地快速启动路径
- 将旧 `app/Admin` 目录正式退场，后台兼容层收敛为 `config/admin.php` + `routes/admin/routes.php`

当前主线判断：

- 订单域和支付域已经有了较稳的重构基础
- 后台替换已经从“评估”进入“并行迁移”，而且后台壳已成为 `/admin` 默认落点
- 安装现代化已基本收口
- 下一阶段的主轴应是“后台壳扩容 + 升级前清障并行推进”

---

## 六大阶段

### 阶段 0：现状固化

目标：

- 确保已经完成的运行时、测试、安装、主题和文档基线始终可复用

退出标准：

- 主线持续可测试
- 文档与仓库状态一致
- 不需要回头补基础设施

### 阶段 1：后台替换准备与并行迁移

目标：

- 将现有旧后台兼容层继续压缩成最小接线层
- 把新后台壳做成可批量迁移的底座

任务：

1. 扩大后台壳资源覆盖面
2. 将旧 Dcat 中的表单保存、批量动作、导入导出继续抽成普通服务
3. 为配置型和操作型页面建立统一的壳页规范
4. 将后台迁移按批次推进，而不是零散替换

退出标准：

- 新后台壳承接一批真实高频后台页面
- 旧 Dcat 仅剩少数复杂页
- 后台替换进入可持续并行迁移阶段

### 阶段 2：支付层现代化收口

目标：

- 将支付层整理成“可保留、可退役、可替换”的稳定结构

任务：

1. 明确保留通道、观察通道、退役通道
2. 完成 PayPal 剩余退场准备
3. 继续稳住 Stripe 的现代 SDK 基线
4. 持续补强支付幂等、签名失败、重复通知等测试

退出标准：

- 支付层生命周期清楚
- 历史通道不再污染默认样例与主路径
- 升级不会被旧支付 SDK 再次反向拖住

### 阶段 3：安装、数据模型与配置现代化

目标：

- 彻底摆脱整包 SQL 安装模型

任务：

1. 补齐 migration / seed 覆盖
2. 清理安装器残余假设
3. 明确 bootstrap、sample、forbidden 三类默认数据
4. 将系统设置逐步转向更长期可维护的配置域

退出标准：

- `install.sql` 只剩历史参考意义
- 新安装完全依赖现代路径
- 环境、种子和管理员创建可重复

### 阶段 4：安全治理专项

目标：

- 专项收掉默认项、后台、支付回调和仓库卫生方面的历史风险

任务：

1. 默认入口与默认项治理
2. 支付签名、金额、幂等与重放保护专项检查
3. 后台权限、上传、模板输入、运行时配置专项检查
4. 凭据、密钥、CI 与仓库卫生清理

退出标准：

- 有明确的安全状态清单
- 每项风险有处理状态，而不是零散记忆

### 阶段 5：升级前清障

目标：

- 为 PHP / Laravel 升级创造干净切口，而不是直接跳版本

任务：

1. 清剩余依赖阻塞
2. 降低旧 Laravel / 旧 Dcat 绑定面
3. 设计现实的分步升级路径
4. 让后台替换与升级路线并轨

退出标准：

- 有现实可执行的升级路径
- 不存在“理论可升、实操升不动”的断层

### 阶段 6：现代化收口

目标：

- 从“遗留系统治理中”切换到“现代维护项目”

任务：

1. 新后台壳成为主承载
2. 支付层生命周期稳定
3. 安装链与部署链统一
4. 输出现代化文档、运维手册和架构说明

退出标准：

- 项目已具备长期维护形态

---

## 默认推进顺序

接下来默认按下面顺序推进：

1. 后台壳继续扩容与操作页落地
2. 支付层继续收口
3. 配置与数据现代化补齐尾巴
4. 安全治理专项清理
5. 升级前清障

---

## 当前执行批次

下面这组任务按顺序连续执行，默认不再逐项等待确认。

### 批次 A：后台壳扩容

1. 继续补商品、订单、支付通道等高频资源的低风险批量动作
2. 继续补系统设置分组和配置型页面的真实编辑入口
3. 继续扩大后台壳对中复杂度资源页的承载面

### 批次 B：后台壳操作页规范

4. 固化批量动作、导入导出、预览、复制等操作页规范
5. 固化操作页头部、表单、提示、返回路径与错误展示规范
6. 让更多旧 Dcat 动作复用新服务层

### 批次 C：后台壳第二批资源

7. 继续推进配置型与中低风险 CRUD 页面
8. 继续推进导入类与批量动作页的后台壳承接方式
9. 继续压缩 Dcat 最小兼容层与旧路由依赖

### 批次 D：回归升级主线

10. 继续清剩余支付升级尾巴
11. 继续整理后台替换与升级前清障的交汇点
12. 为 Laravel / PHP 升级准备更干净的切口

---

## 批次执行规则

每个批次内遵循：

1. 选择一个明确整改点
2. 实施代码或文档变更
3. 补测试或验证
4. 更新升级日志
5. 在稳定节点同步 GitHub `master`

这意味着后续推进默认会以“阶段任务包”的形式连续执行，而不是每做一个小步骤就等待确认。
